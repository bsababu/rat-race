note
    description : "the constants variables"
    date        : "$Date$"
    revision    : "$Revision$"

class
    CONSTANTS

inherit
    ARGUMENTS

create
    make

-- defining the constants
feature
    make
        do
        
            -- hit_points: INTEGER := 15 -- initial hp 
            -- board_squares : ARRAY[INTEGER]-- initial squares
            -- players_turn : ARRAY[INTEGER] -- having 2 to 6 players
            --dice: INTEGER
        end
end
